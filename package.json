{
  "name": "reopt-api-client",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node reopt-single-scenario.ts",
    "start": "node dist/reopt-single-scenario.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["reopt", "api", "renewable", "energy"],
  "author": "",
  "license": "ISC",
  "description": "TypeScript client for REopt API single scenario example",
  "dependencies": {
    "axios": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.14",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
